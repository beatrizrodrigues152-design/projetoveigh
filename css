/* Reset básico */
* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
  background: #121212;
  color: #f0f0f0;
  line-height: 1.6;
  min-height: 100vh;
  display: flex;
  justify-content: center;
  padding: 20px;
}

.container {
  max-width: 900px;
  width: 100%;
  background-color: #1e1e1e;
  padding: 30px 40px;
  border-radius: 10px;
  box-shadow: 0 0 15px rgba(128, 0, 128, 0.5);
}

/* Cabeçalho */
header {
  text-align: center;
  margin-bottom: 40px;
}

header img {
  border-radius: 50%;
  border: 3px solid #800080; /* roxo */
  margin-bottom: 15px;
  transition: transform 0.3s ease;
}

header img:hover {
  transform: scale(1.05);
}

header p {
  font-style: italic;
  font-size: 1.2rem;
  color: #800080; /* roxo */
}

/* Seções principais */
main section {
  margin-bottom: 40px;
}

main section img {
  width: 100%;
  max-height: 350px;
  object-fit: cover;
  border-radius: 10px;
  margin-bottom: 15px;
  box-shadow: 0 4px 10px rgba(0, 0, 0, 0.5);
}

h2 {
  color: #800080; /* roxo */
  margin-bottom: 15px;
  border-bottom: 2px solid #800080;
  padding-bottom: 5px;
}

p {
  margin-bottom: 15px;
  font-size: 1rem;
}

/* Listas */
ul {
  list-style: inside disc;
  margin-left: 20px;
  margin-bottom: 15px;
}

li {
  margin-bottom: 8px;
}

/* Blocos de citação */
blockquote {
  background-color: #2c2c2c;
  border-left: 5px solid #800080; /* roxo */
  padding: 15px 20px;
  margin: 20px 0;
  font-style: italic;
  color: #c39bd3; /* tom roxo claro */
  border-radius: 5px;
}

blockquote p {
  margin-top: 8px;
  font-style: normal;
  text-align: right;
  color: #800080; /* roxo */
  font-weight: bold;
}

/* Linha horizontal */
hr {
  border: 0;
  height: 1px;
  background: #333;
  margin: 40px 0;
}

/* Rodapé */
footer {
  text-align: center;
  padding-top: 20px;
  border-top: 1px solid #333;
  font-size: 0.9rem;
  color: #aaa;
}

footer h2 {
  color: #800080; /* roxo */
  margin-bottom: 10px;
}

footer a {
  color: #800080; /* roxo */
  text-decoration: none;
  transition: color 0.3s ease;
}

footer a:hover {
  color: #c39bd3; /* roxo claro */
}

/* Links e imagens no footer */
footer img {
  margin-top: 10px;
  width: 60px;
  filter: brightness(0) invert(1);
  transition: filter 0.3s ease;
}

footer img:hover {
  filter: brightness(0) invert(0.7) sepia(1) saturate(5) hue-rotate(270deg);
}

/* Responsividade simples */
@media (max-width: 600px) {
  .container {
    padding: 20px;
  }

  header img {
    height: 200px;
    width: 200px;
  }

  main section img {
    max-height: 200px;
  }
}
